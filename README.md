# Assignment-10
This project demonstrates:

Basic web scraping

HTTP headers handling

HTML parsing using lxml

Object-oriented design in Python

🚀 Features

Extracts product title

Extracts current price

Uses a browser-like User-Agent to avoid basic bot blocking

Clean OOP structure for future extension (alerts, multiple products, etc.)
